<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MyTheme" parent="Theme.AppCompat.DayNight">
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="colorControlHighlight">@color/colorAccent</item>
    </style>

    <style name="macauto" parent="@style/Theme.AppCompat.Light.DarkActionBar">
        <!--item name="android:homeAsUpIndicator">@drawable/ic_chevron_left_white_24dp</item-->
        <item name="android:actionMenuTextColor">@color/menuTextWhite</item>
        <item name="titleTextColor">@color/menuTextWhite</item>
    </style>

</resources>
